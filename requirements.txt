# GraphRAG项目依赖文件 - 优化版本
# Python版本要求: >=3.10,<3.12
# 
# 解决方案说明:
# 1. 使用unstructured==0.10.30 解决版本兼容性问题
# 2. 移除了重复和不必要的PDF处理库
# 3. 优化版本范围，提高兼容性
# 4. 移除了项目中未使用的依赖

# === 核心Web框架 ===
Flask>=3.0.0,<4.0.0
Flask-CORS>=4.0.0,<5.0.0

# === 数据库相关 ===
SQLAlchemy>=2.0.0,<3.0.0
PyMySQL>=1.1.0,<2.0.0
pymilvus>=2.3.0,<3.0.0
neo4j>=5.15.0,<6.0.0
# 修复protobuf版本兼容性问题 - 确保与milvus兼容
protobuf>=5.27.0,<6.0.0

# === AI和向量处理 ===
torch>=2.1.1,<3.0.0
torchvision>=0.16.1,<1.0.0
transformers>=4.36.0,<5.0.0
sentence-transformers>=2.2.2,<3.0.0
numpy>=1.24.3,<2.0.0

# === 文档处理（核心） ===
# 使用经过测试的稳定版本组合
unstructured==0.10.30
unstructured[pdf]==0.10.30

# === 基础工具 ===
PyYAML>=6.0.0,<7.0.0
requests>=2.31.0,<3.0.0
tqdm>=4.66.0,<5.0.0
Pillow>=10.1.0,<11.0.0
matplotlib>=3.6.0,<4.0.0

# === 安全相关 ===
cryptography>=42.0.0,<46.0.0

# === 数据处理 ===
pandas>=2.1.0,<3.0.0

# === 开发和测试工具（可选） ===
# 如果需要开发功能，取消注释：
# pytest>=7.4.0,<8.0.0
# black>=23.0.0,<24.0.0  
# flake8>=6.0.0,<7.0.0

# === 额外功能（按需启用） ===
# 如果需要Office文档处理：
# python-docx>=1.1.0,<2.0.0
# openpyxl>=3.1.0,<4.0.0
# python-pptx>=0.6.0,<1.0.0

# 如果需要OCR功能：
# pytesseract>=0.3.10,<1.0.0  
# easyocr>=1.7.0,<2.0.0

# 如果需要中文文本处理：
# jieba>=0.42.0,<1.0.0

# 如果需要Redis缓存：
# redis>=5.0.0,<6.0.0